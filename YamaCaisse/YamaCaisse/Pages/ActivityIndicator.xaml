<?xml version="1.0" encoding="UTF-8"?>
    <StackLayout xmlns="http://xamarin.com/schemas/2014/forms" 
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
                 x:Class="YamaCaisse.Pages.ActivityIndicator" IsVisible="{Binding IsBusy}" Padding="12"
               BackgroundColor="#80000000" Opacity="1"  AbsoluteLayout.LayoutFlags="PositionProportional"
                 AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1" >
        <StackLayout HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand" Orientation="Vertical" AbsoluteLayout.LayoutFlags="YProportional" BackgroundColor="#9F318D" AbsoluteLayout.LayoutBounds="0.5,0.5,-1,-1" Opacity="0,50" Padding="12">
            <ActivityIndicator IsRunning="{Binding IsBusy}" Color="White" />
            <Label Text="Chargement" HorizontalOptions="Center" TextColor="White" />
        </StackLayout>
    </StackLayout>